{{- if .Values.secrets.createStub }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.backend.secretName }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "canvas.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
type: Opaque
data: {}
{{- else }}
# Secret values are intentionally not rendered by this chart.
# Create the secret named {{ .Values.backend.secretName }} with the keys referenced by deployment.yaml.
{{- end }}
