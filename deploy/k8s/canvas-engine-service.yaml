apiVersion: v1
kind: Service
metadata:
  name: canvas-engine
  namespace: canvas
  labels:
    app.kubernetes.io/name: canvas-engine
    app.kubernetes.io/part-of: marketing-canvas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: canvas-engine
  ports:
    - name: http
      port: 8080
      targetPort: http
