# Completed Architecture Packages

This folder contains completed architecture design packages that were materialized from `docs/architecture/todo/`.

- `specs/`: priority-prefixed architecture specs.
- `plans/`: matching priority-prefixed implementation plans.

Use `../../specs/README.md` and `../../plans/README.md` as the navigation entry points. Keep source traceability in `../../todo/coverage-matrix.md`.
