# 多级缓存 SDK Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将多级缓存模式抽离为独立 Maven 模块 `canvas-cache-sdk`，实现泛型 L1+L2+L3、双轨 API（同步 + 响应式）、方法级显式注解（读缓存/失效/写入）、四大策略枚举、三大防护（穿透/击穿/雪崩）、Micrometer 埋点、测试工具，并将 `CanvasConfigCache` 迁移至 SDK。

**Architecture:** Maven 子模块（无业务依赖）→ Strategy 枚举配置行为 → `TieredCacheImpl` 共享 L1/L2 → `.asReactive()` 视图复用实例 → `TieredCacheManager` 管理 Pub/Sub + 注册表 → Spring AOP 方法级注解调用 `get(key, loaderOverride)` → `@TestTieredCache` 测试自动装配。

**Tech Stack:** Java 21（虚拟线程）, Caffeine, Spring Data Redis（同步 + 响应式）, Spring AOP + SpEL, Jackson, Micrometer, JUnit 5 + Mockito

---

## File Map

| Action | Path |
|--------|------|
| Create | `backend/pom.xml` |
| Create | `backend/canvas-cache-sdk/pom.xml` |
| Create | `src/main/java/org/chovy/cache/TieredCache.java` |
| Create | `src/main/java/org/chovy/cache/ReactiveTieredCache.java` |
| Create | `src/main/java/org/chovy/cache/strategy/*.java`（4 个枚举） |
| Create | `src/main/java/org/chovy/cache/TieredCacheImpl.java` |
| Create | `src/main/java/org/chovy/cache/TieredCacheBuilder.java` |
| Create | `src/main/java/org/chovy/cache/TieredCacheManager.java` |
| Create | `src/main/java/org/chovy/cache/annotation/TieredCached.java` |
| Create | `src/main/java/org/chovy/cache/annotation/TieredCacheEvict.java` |
| Create | `src/main/java/org/chovy/cache/annotation/TieredCachePut.java` |
| Create | `src/main/java/org/chovy/cache/aop/TieredCacheAspect.java` |
| Create | `src/main/java/org/chovy/cache/aop/AnnotationCacheResolver.java` |
| Create | `src/main/java/org/chovy/cache/aop/SpelKeyEvaluator.java` |
| Create | `src/main/java/org/chovy/cache/autoconfigure/TieredCacheAutoConfiguration.java` |
| Create | `src/main/java/org/chovy/cache/testing/InMemoryTieredCache.java` |
| Create | `src/main/java/org/chovy/cache/testing/TestTieredCacheConfig.java` |
| Create | `src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` |
| Create | `src/test/java/org/chovy/cache/TieredCacheTest.java` |
| Create | `src/test/java/org/chovy/cache/aop/TieredCacheAspectTest.java` |
| Modify | `backend/canvas-engine/pom.xml` |
| Create | `canvas-engine/.../config/CacheConfig.java` |
| Modify | `canvas-engine/.../infra/cache/CanvasConfigCache.java` |

SDK 路径前缀：`backend/canvas-cache-sdk/src/main/java/org/chovy/cache/`

---

## Task 1：Maven 模块骨架

- [ ] **Step 1：创建聚合父 pom（backend/pom.xml）**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.chovy</groupId>
    <artifactId>canvas-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <modules>
        <module>canvas-cache-sdk</module>
        <module>canvas-engine</module>
    </modules>
</project>
```

- [ ] **Step 2：创建 canvas-cache-sdk/pom.xml**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>
    <groupId>org.chovy</groupId>
    <artifactId>canvas-cache-sdk</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <properties>
        <java.version>21</java.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
```

- [ ] **Step 3：canvas-engine/pom.xml 添加 SDK 依赖**

```xml
<dependency>
    <groupId>org.chovy</groupId>
    <artifactId>canvas-cache-sdk</artifactId>
    <version>1.0.0-SNAPSHOT</version>
</dependency>
```

- [ ] **Step 4：编译验证**

```bash
cd backend && mvn compile -q
```

Expected: `BUILD SUCCESS`

- [ ] **Step 5：Commit**

```bash
git add backend/pom.xml backend/canvas-cache-sdk/pom.xml backend/canvas-engine/pom.xml
git commit -m "chore: add canvas-cache-sdk Maven module and parent pom"
```

---

## Task 2：策略枚举（4 个）

**Files:** `strategy/` 包下 4 个枚举

- [ ] **Step 1：LoaderFailureStrategy.java**

```java
package org.chovy.cache.strategy;

public enum LoaderFailureStrategy {
    /** 直接抛出异常（默认） */
    THROW,
    /** L1 有旧值则返回旧值，否则抛出（refreshAfterWrite 刷新失败时保留旧值） */
    RETURN_STALE,
    /** 静默返回 Optional.empty()（慎用，会掩盖问题） */
    RETURN_EMPTY
}
```

- [ ] **Step 2：RedisFailureStrategy.java**

```java
package org.chovy.cache.strategy;

public enum RedisFailureStrategy {
    /** 降级穿透：读时穿 L3，写时静默忽略打 warn 日志（默认） */
    FALLTHROUGH,
    /** 快速失败：Redis 异常直接抛出，交上层熔断器处理 */
    FAIL_FAST
}
```

- [ ] **Step 3：DeserializeFailureStrategy.java**

```java
package org.chovy.cache.strategy;

public enum DeserializeFailureStrategy {
    /** 删除坏 key + 穿透 L3（自愈，默认） */
    FALLTHROUGH_TO_L3,
    /** 抛出异常（适合排查期） */
    THROW
}
```

- [ ] **Step 4：ConsistencyStrategy.java**

```java
package org.chovy.cache.strategy;

public enum ConsistencyStrategy {
    /** 写后失效（Cache-Aside），默认 */
    EVENTUAL,
    /** 双删：删 L2 → 写 DB → 延迟再删 L2，收缩竞态窗口 */
    DOUBLE_DELETE
}
```

- [ ] **Step 5：编译验证**

```bash
cd backend && mvn compile -pl canvas-cache-sdk -q
```

Expected: `BUILD SUCCESS`

- [ ] **Step 6：Commit**

```bash
git add backend/canvas-cache-sdk/src/main/java/org/chovy/cache/strategy/
git commit -m "feat(cache-sdk): add strategy enums (Loader/Redis/Deserialize/Consistency)"
```

---

## Task 3：核心接口

**Files:** `TieredCache.java`, `ReactiveTieredCache.java`

- [ ] **Step 1：TieredCache.java**

```java
package org.chovy.cache;

import java.util.NoSuchElementException;
import java.util.Optional;
import java.util.function.Supplier;

public interface TieredCache<K, V> {

    String name();

    /** L1 → L2 → Loader，自动回填上级缓存 */
    Optional<V> get(K key);

    /** L1 → L2 → 本次调用提供的 Loader。用于方法级注解，不影响 Builder 默认 Loader。 */
    Optional<V> get(K key, Supplier<V> loaderOverride);

    default V getOrThrow(K key) {
        return get(key).orElseThrow(() ->
                new NoSuchElementException("Cache miss: " + name() + " key=" + key));
    }

    /** 写 L2 → 写 L1（L2 失败则 L1 不写，保证 L1 ⊆ L2） */
    void put(K key, V value);

    /** 清除 L1 + L2，Pub/Sub 广播所有实例 */
    void invalidate(K key);

    /**
     * 双删一致性写：删 L2 → 执行 writeAction（DB 写）→ sleep(delayMs) → 再删 L2。
     * 用于收缩 Cache-Aside 的竞态窗口。
     */
    void safeWrite(K key, Runnable writeAction, long delayMs);

    /** 强制从 Loader 重新加载，更新 L1 + L2 */
    void refresh(K key);

    /** 切换到响应式视图，共享同一 L1/L2 实例 */
    ReactiveTieredCache<K, V> asReactive();
}
```

- [ ] **Step 2：ReactiveTieredCache.java**

```java
package org.chovy.cache;

import reactor.core.publisher.Mono;
import java.util.Optional;

/**
 * 响应式缓存视图。L1 查询仍为同步（Caffeine），L2 走 ReactiveStringRedisTemplate，
 * Loader 通过 Mono.fromCallable + boundedElastic 包装。
 */
public interface ReactiveTieredCache<K, V> {
    Mono<Optional<V>> get(K key);
    Mono<Void> put(K key, V value);
    Mono<Void> invalidate(K key);
    Mono<Void> refresh(K key);
}
```

- [ ] **Step 3：Commit**

```bash
git add backend/canvas-cache-sdk/src/main/java/org/chovy/cache/TieredCache.java \
        backend/canvas-cache-sdk/src/main/java/org/chovy/cache/ReactiveTieredCache.java
git commit -m "feat(cache-sdk): add TieredCache and ReactiveTieredCache interfaces"
```

---

## Task 4：TieredCacheImpl（含三大防护 + 双轨 API）

**Files:** `TieredCacheImpl.java`

- [ ] **Step 1：创建完整实现**

```java
package org.chovy.cache;

import com.fasterxml.jackson.databind.JavaType;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.github.benmanes.caffeine.cache.CacheLoader;
import com.github.benmanes.caffeine.cache.Caffeine;
import com.github.benmanes.caffeine.cache.LoadingCache;
import io.micrometer.core.instrument.MeterRegistry;
import io.micrometer.core.instrument.Tags;
import lombok.Getter;
import lombok.extern.slf4j.Slf4j;
import org.chovy.cache.strategy.*;
import org.springframework.data.redis.core.ReactiveStringRedisTemplate;
import org.springframework.data.redis.core.StringRedisTemplate;
import reactor.core.publisher.Mono;
import reactor.core.scheduler.Schedulers;

import java.time.Duration;
import java.util.Optional;
import java.util.concurrent.*;
import java.util.function.Function;
import java.util.function.Supplier;

@Slf4j
public class TieredCacheImpl<K, V> implements TieredCache<K, V> {

    // ── 配置 ──────────────────────────────────────────────────────
    @Getter private final String name;
    private final String l2KeyPrefix;
    private final Duration l2Ttl;
    private final double l2TtlJitter;
    private final int keySchemaVersion;
    private final Duration nullValueTtl;
    private final boolean hotspotProtection;
    private final LoaderFailureStrategy loaderFailure;
    private final RedisFailureStrategy redisReadFailure;
    private final RedisFailureStrategy redisWriteFailure;
    private final DeserializeFailureStrategy deserializeFailure;
    private final Function<K, V> loader;
    private final JavaType valueJavaType;
    private final ObjectMapper objectMapper;

    // ── 基础设施 ───────────────────────────────────────────────────
    private final StringRedisTemplate redis;
    private final ReactiveStringRedisTemplate reactiveRedis;
    private final LoadingCache<K, Optional<V>> l1;
    private final ReactiveTieredCache<K, V> reactiveView;
    private static final String NULL_SENTINEL = "__NULL__";

    // ── Micrometer 计数器 ─────────────────────────────────────────
    private io.micrometer.core.instrument.Counter l1Hits, l2Hits, l3Hits;
    private io.micrometer.core.instrument.Counter l1Misses, l2Misses;
    private io.micrometer.core.instrument.Timer loadTimer;
    private io.micrometer.core.instrument.Counter penetrationHits, hotspotWaits, loaderFailures;

    TieredCacheImpl(String name, int l1MaxSize, Duration l1RefreshAfterWrite,
                    String l2KeyPrefix, Duration l2Ttl, double l2TtlJitter,
                    int keySchemaVersion, Duration nullValueTtl, boolean hotspotProtection,
                    LoaderFailureStrategy loaderFailure,
                    RedisFailureStrategy redisReadFailure, RedisFailureStrategy redisWriteFailure,
                    DeserializeFailureStrategy deserializeFailure,
                    Function<K, V> loader, JavaType valueJavaType, ObjectMapper objectMapper,
                    StringRedisTemplate redis, ReactiveStringRedisTemplate reactiveRedis,
                    MeterRegistry meterRegistry) {
        this.name = name;
        this.l2KeyPrefix = l2KeyPrefix;
        this.l2Ttl = l2Ttl;
        this.l2TtlJitter = l2TtlJitter;
        this.keySchemaVersion = keySchemaVersion;
        this.nullValueTtl = nullValueTtl;
        this.hotspotProtection = hotspotProtection;
        this.loaderFailure = loaderFailure;
        this.redisReadFailure = redisReadFailure;
        this.redisWriteFailure = redisWriteFailure;
        this.deserializeFailure = deserializeFailure;
        this.loader = loader;
        this.valueJavaType = valueJavaType;
        this.objectMapper = objectMapper;
        this.redis = redis;
        this.reactiveRedis = reactiveRedis;

        // L1：refreshAfterWrite + 虚拟线程执行器
        this.l1 = Caffeine.newBuilder()
                .maximumSize(l1MaxSize)
                .refreshAfterWrite(l1RefreshAfterWrite)
                .executor(Executors.newVirtualThreadPerTaskExecutor())
                .build(buildCacheLoader());

        // 响应式视图（共享 L1 实例）
        this.reactiveView = new ReactiveTieredCacheView<>(this);

        // 指标注册
        if (meterRegistry != null) registerMetrics(meterRegistry);
    }

    // ── TieredCache 实现 ───────────────────────────────────────────

    @Override
    public Optional<V> get(K key) {
        return get(key, () -> loader.apply(key));
    }

    @Override
    public Optional<V> get(K key, Supplier<V> loaderOverride) {
        Optional<V> l1Val = l1.getIfPresent(key);
        if (l1Val != null) { countHit("L1"); return l1Val; }
        countMiss("L1");

        Optional<V> result = loadFromL2ThenL3(key, loaderOverride);
        if (result != null) l1.put(key, result);
        return result != null ? result : Optional.empty();
    }

    @Override
    public void put(K key, V value) {
        // L2 先写，成功后写 L1（保证 L1 ⊆ L2）
        boolean l2Ok = writeL2(key, value);
        if (l2Ok) l1.put(key, Optional.ofNullable(value));
    }

    @Override
    public void invalidate(K key) {
        l1.invalidate(key);
        deleteL2(key);
        publishInvalidate(keyToString(key));
        log.debug("[TIERED_CACHE][{}] invalidate key={}", name, key);
    }

    @Override
    public void safeWrite(K key, Runnable writeAction, long delayMs) {
        // 双删：删 L2 → 写 DB → sleep → 再删 L2
        deleteL2(key);
        l1.invalidate(key);
        publishInvalidate(keyToString(key));
        try {
            writeAction.run();
            if (delayMs > 0) Thread.sleep(delayMs);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        }
        deleteL2(key);
        publishInvalidate(keyToString(key));
    }

    @Override
    public void refresh(K key) {
        l1.invalidate(key);
        deleteL2(key);
        l1.get(key); // 触发重新加载
    }

    @Override
    public ReactiveTieredCache<K, V> asReactive() { return reactiveView; }

    // ── Pub/Sub 回调（由 TieredCacheManager 调用）────────────────
    void onInvalidateBroadcast(String rawKey) {
        l1.asMap().keySet().removeIf(k -> keyToString(k).equals(rawKey));
        log.debug("[TIERED_CACHE][{}] L1 evict from pub/sub rawKey={}", name, rawKey);
    }

    // ── 内部加载逻辑 ───────────────────────────────────────────────

    private CacheLoader<K, Optional<V>> buildCacheLoader() {
        return new CacheLoader<>() {
            @Override public Optional<V> load(K key) {
                return loadFromL2ThenL3(key, () -> loader.apply(key));
            }
            @Override public Optional<V> reload(K key, Optional<V> old) {
                // 续期：优先 L2，避免穿透 L3
                try {
                    String json = redis.opsForValue().get(l2Key(key));
                    if (json != null) {
                        if (NULL_SENTINEL.equals(json)) return Optional.empty();
                        log.debug("[TIERED_CACHE][{}] L1 renewed from L2 key={}", name, key);
                        return Optional.ofNullable(deserialize(json));
                    }
                } catch (Exception e) {
                    if (redisReadFailure == RedisFailureStrategy.FAIL_FAST) throw e;
                    log.warn("[TIERED_CACHE][{}] Redis read failed on refresh, fallthrough: {}", name, e.getMessage());
                }
                return loadFromL3(key, old, () -> loader.apply(key));
            }
        };
    }

    private Optional<V> loadFromL2ThenL3(K key, Supplier<V> effectiveLoader) {
        try {
            String json = redis.opsForValue().get(l2Key(key));
            if (json != null) {
                if (NULL_SENTINEL.equals(json)) { countHit("L2"); penetrationHits(); return Optional.empty(); }
                V val = deserializeWithFallback(key, json);
                if (val != null) { countHit("L2"); return Optional.of(val); }
            }
            countMiss("L2");
        } catch (Exception e) {
            if (redisReadFailure == RedisFailureStrategy.FAIL_FAST) throw new RuntimeException(e);
            log.warn("[TIERED_CACHE][{}] Redis read failed, fallthrough to L3: {}", name, e.getMessage());
        }
        return hotspotProtection
                ? loadFromL3WithLock(key, null, effectiveLoader)
                : loadFromL3(key, null, effectiveLoader);
    }

    private Optional<V> loadFromL3(K key, Optional<V> staleValue, Supplier<V> effectiveLoader) {
        try {
            countHit("L3");
            V value = loadTimer != null
                    ? loadTimer.record(effectiveLoader::get)
                    : effectiveLoader.get();
            populateL2(key, value);
            return Optional.ofNullable(value);
        } catch (Exception e) {
            loaderFailures();
            return switch (loaderFailure) {
                case THROW -> throw e instanceof RuntimeException re ? re : new RuntimeException(e);
                case RETURN_STALE -> staleValue != null ? staleValue : Optional.empty();
                case RETURN_EMPTY -> Optional.empty();
            };
        }
    }

    private Optional<V> loadFromL3WithLock(K key, Optional<V> staleValue, Supplier<V> effectiveLoader) {
        String lockKey = "cache:lock:" + l2Key(key);
        boolean locked = Boolean.TRUE.equals(
                redis.opsForValue().setIfAbsent(lockKey, "1", Duration.ofSeconds(30)));
        if (locked) {
            try { return loadFromL3(key, staleValue, effectiveLoader); }
            finally { redis.delete(lockKey); }
        }
        return waitAndRetryL2(key, staleValue, effectiveLoader);
    }

    private Optional<V> waitAndRetryL2(K key, Optional<V> staleValue, Supplier<V> effectiveLoader) {
        hotspotWaits();
        for (int i = 0; i < 10; i++) {
            try { Thread.sleep(50); } catch (InterruptedException e) { Thread.currentThread().interrupt(); break; }
            try {
                String json = redis.opsForValue().get(l2Key(key));
                if (json != null) return Optional.ofNullable(NULL_SENTINEL.equals(json) ? null : deserialize(json));
            } catch (Exception ignored) {}
        }
        log.warn("[TIERED_CACHE][{}] lock wait timeout, fallback direct L3 key={}", name, key);
        return loadFromL3(key, staleValue, effectiveLoader);
    }

    // ── L2 操作 ────────────────────────────────────────────────────

    private void populateL2(K key, V value) {
        writeL2(key, value);
    }

    private boolean writeL2(K key, V value) {
        try {
            if (value == null) {
                redis.opsForValue().set(l2Key(key), NULL_SENTINEL, nullValueTtl);
            } else {
                redis.opsForValue().set(l2Key(key), serialize(value), actualL2Ttl());
            }
            return true;
        } catch (Exception e) {
            if (redisWriteFailure == RedisFailureStrategy.FAIL_FAST) throw new RuntimeException(e);
            log.warn("[TIERED_CACHE][{}] Redis write failed, L1 not written: {}", name, e.getMessage());
            return false;
        }
    }

    private void deleteL2(K key) {
        try { redis.delete(l2Key(key)); }
        catch (Exception e) {
            if (redisWriteFailure == RedisFailureStrategy.FAIL_FAST) throw new RuntimeException(e);
            log.warn("[TIERED_CACHE][{}] Redis delete failed: {}", name, e.getMessage());
        }
    }

    private void publishInvalidate(String rawKey) {
        try {
            redis.convertAndSend(invalidateChannel(), rawKey);
        } catch (Exception e) {
            log.warn("[TIERED_CACHE][{}] Pub/Sub publish failed (L1 may be stale until refresh): {}", name, e.getMessage());
        }
    }

    // ── 工具方法 ───────────────────────────────────────────────────

    String invalidateChannel() { return "tiered-cache:" + name + ":invalidate"; }
    private String l2Key(K key) { return l2KeyPrefix + "v" + keySchemaVersion + ":" + keyToString(key); }
    private String keyToString(K key) { return key.toString(); }

    private Duration actualL2Ttl() {
        if (l2TtlJitter <= 0) return l2Ttl;
        long jitterMs = (long)(l2Ttl.toMillis()
                * ThreadLocalRandom.current().nextDouble(0, l2TtlJitter));
        return l2Ttl.plus(Duration.ofMillis(jitterMs));
    }

    private String serialize(V value) {
        try { return objectMapper.writeValueAsString(value); }
        catch (Exception e) { throw new IllegalStateException("Cache serialize failed", e); }
    }

    private V deserialize(String json) {
        try { return objectMapper.readValue(json, valueJavaType); }
        catch (Exception e) { throw new IllegalStateException("Cache deserialize failed", e); }
    }

    private V deserializeWithFallback(K key, String json) {
        try { return objectMapper.readValue(json, valueJavaType); }
        catch (Exception e) {
            if (deserializeFailure == DeserializeFailureStrategy.THROW) throw new IllegalStateException(e);
            log.warn("[TIERED_CACHE][{}] deserialize failed, deleting L2 key={}: {}", name, key, e.getMessage());
            deleteL2(key); // 自愈：删坏数据
            return null;
        }
    }

    // ── Micrometer 埋点 ────────────────────────────────────────────

    private void registerMetrics(MeterRegistry registry) {
        Tags tags = Tags.of("cache", name);
        l1Hits    = registry.counter("tiered_cache_hits_total",   tags.and("level", "L1"));
        l2Hits    = registry.counter("tiered_cache_hits_total",   tags.and("level", "L2"));
        l3Hits    = registry.counter("tiered_cache_hits_total",   tags.and("level", "L3"));
        l1Misses  = registry.counter("tiered_cache_misses_total", tags.and("level", "L1"));
        l2Misses  = registry.counter("tiered_cache_misses_total", tags.and("level", "L2"));
        loadTimer = registry.timer("tiered_cache_load_duration_seconds", tags);
        penetrationHits = registry.counter("tiered_cache_penetration_total", tags);
        hotspotWaits    = registry.counter("tiered_cache_hotspot_lock_wait_total", tags);
        loaderFailures  = registry.counter("tiered_cache_loader_failure_total", tags);
    }

    private void countHit(String level)  {
        if ("L1".equals(level) && l1Hits != null) l1Hits.increment();
        else if ("L2".equals(level) && l2Hits != null) l2Hits.increment();
        else if ("L3".equals(level) && l3Hits != null) l3Hits.increment();
    }
    private void countMiss(String level) {
        if ("L1".equals(level) && l1Misses != null) l1Misses.increment();
        else if ("L2".equals(level) && l2Misses != null) l2Misses.increment();
    }
    private void penetrationHits() { if (penetrationHits != null) penetrationHits.increment(); }
    private void hotspotWaits()    { if (hotspotWaits != null) hotspotWaits.increment(); }
    private void loaderFailures()  { if (loaderFailures != null) loaderFailures.increment(); }
}
```

- [ ] **Step 2：创建 ReactiveTieredCacheView（TieredCacheImpl 内部类或同包类）**

```java
package org.chovy.cache;

import reactor.core.publisher.Mono;
import reactor.core.scheduler.Schedulers;
import java.util.Optional;

/** 响应式视图：L1 同步查，L2 走 ReactiveStringRedisTemplate，Loader 用 boundedElastic 包装 */
class ReactiveTieredCacheView<K, V> implements ReactiveTieredCache<K, V> {
    private final TieredCacheImpl<K, V> delegate;

    ReactiveTieredCacheView(TieredCacheImpl<K, V> delegate) {
        this.delegate = delegate;
    }

    @Override
    public Mono<Optional<V>> get(K key) {
        // L1 同步查，命中直接返回
        Optional<V> l1Val = delegate.l1.getIfPresent(key);
        if (l1Val != null) return Mono.just(l1Val);
        // L2+L3 走 boundedElastic（阻塞操作隔离到弹性线程池）
        return Mono.fromCallable(() -> delegate.get(key))
                .subscribeOn(Schedulers.boundedElastic());
    }

    @Override
    public Mono<Void> put(K key, V value) {
        return Mono.fromRunnable(() -> delegate.put(key, value))
                .subscribeOn(Schedulers.boundedElastic())
                .then();
    }

    @Override
    public Mono<Void> invalidate(K key) {
        return Mono.fromRunnable(() -> delegate.invalidate(key))
                .subscribeOn(Schedulers.boundedElastic())
                .then();
    }

    @Override
    public Mono<Void> refresh(K key) {
        return Mono.fromRunnable(() -> delegate.refresh(key))
                .subscribeOn(Schedulers.boundedElastic())
                .then();
    }
}
```

- [ ] **Step 3：编译验证**

```bash
cd backend && mvn compile -pl canvas-cache-sdk -q
```

Expected: `BUILD SUCCESS`

- [ ] **Step 4：Commit**

```bash
git add backend/canvas-cache-sdk/src/main/java/org/chovy/cache/
git commit -m "feat(cache-sdk): implement TieredCacheImpl with dual-API, 3 protections, strategies, metrics"
```

---

## Task 5：TieredCacheBuilder

**Files:** `TieredCacheBuilder.java`

- [ ] **Step 1：创建完整 Builder**

```java
package org.chovy.cache;

import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.JavaType;
import com.fasterxml.jackson.databind.ObjectMapper;
import io.micrometer.core.instrument.MeterRegistry;
import lombok.AccessLevel;
import lombok.NoArgsConstructor;
import org.chovy.cache.strategy.*;
import org.springframework.data.redis.core.ReactiveStringRedisTemplate;
import org.springframework.data.redis.core.StringRedisTemplate;

import java.time.Duration;
import java.util.function.Function;

@NoArgsConstructor(access = AccessLevel.PRIVATE)
public class TieredCacheBuilder<K, V> {

    private String name;
    private int l1MaxSize = 1000;
    private Duration l1RefreshAfterWrite = Duration.ofHours(1);
    private String l2KeyPrefix = "cache:";
    private Duration l2Ttl = Duration.ofHours(24);
    private double l2TtlJitter = 0.1;
    private int keySchemaVersion = 1;
    private Duration nullValueTtl = Duration.ofMinutes(5);
    private boolean hotspotProtection = false;
    private LoaderFailureStrategy loaderFailure = LoaderFailureStrategy.THROW;
    private RedisFailureStrategy redisReadFailure = RedisFailureStrategy.FALLTHROUGH;
    private RedisFailureStrategy redisWriteFailure = RedisFailureStrategy.FALLTHROUGH;
    private DeserializeFailureStrategy deserializeFailure = DeserializeFailureStrategy.FALLTHROUGH_TO_L3;
    private Function<K, V> loader;
    private JavaType valueJavaType;
    private ObjectMapper objectMapper = new ObjectMapper();
    private boolean enableMetrics = true;
    private MeterRegistry meterRegistry;

    public static <K, V> TieredCacheBuilder<K, V> builder() { return new TieredCacheBuilder<>(); }

    public TieredCacheBuilder<K, V> name(String v)                        { this.name = v; return this; }
    public TieredCacheBuilder<K, V> l1MaxSize(int v)                      { this.l1MaxSize = v; return this; }
    public TieredCacheBuilder<K, V> l1RefreshAfterWrite(Duration v)       { this.l1RefreshAfterWrite = v; return this; }
    public TieredCacheBuilder<K, V> l2KeyPrefix(String v)                 { this.l2KeyPrefix = v; return this; }
    public TieredCacheBuilder<K, V> l2Ttl(Duration v)                     { this.l2Ttl = v; return this; }
    public TieredCacheBuilder<K, V> l2TtlJitter(double v)                 { this.l2TtlJitter = v; return this; }
    public TieredCacheBuilder<K, V> keySchemaVersion(int v)               { this.keySchemaVersion = v; return this; }
    public TieredCacheBuilder<K, V> nullValueTtl(Duration v)              { this.nullValueTtl = v; return this; }
    public TieredCacheBuilder<K, V> hotspotProtection(boolean v)          { this.hotspotProtection = v; return this; }
    public TieredCacheBuilder<K, V> onLoaderFailure(LoaderFailureStrategy v)       { this.loaderFailure = v; return this; }
    public TieredCacheBuilder<K, V> onRedisReadFailure(RedisFailureStrategy v)     { this.redisReadFailure = v; return this; }
    public TieredCacheBuilder<K, V> onRedisWriteFailure(RedisFailureStrategy v)    { this.redisWriteFailure = v; return this; }
    public TieredCacheBuilder<K, V> onDeserializeFailure(DeserializeFailureStrategy v) { this.deserializeFailure = v; return this; }
    public TieredCacheBuilder<K, V> loader(Function<K, V> v)              { this.loader = v; return this; }
    public TieredCacheBuilder<K, V> valueType(Class<V> clazz)             { this.valueJavaType = objectMapper.constructType(clazz); return this; }
    public TieredCacheBuilder<K, V> valueType(TypeReference<V> ref)       { this.valueJavaType = objectMapper.constructType(ref.getType()); return this; }
    public TieredCacheBuilder<K, V> objectMapper(ObjectMapper v)          { this.objectMapper = v; return this; }
    public TieredCacheBuilder<K, V> enableMetrics(boolean v)              { this.enableMetrics = v; return this; }
    public TieredCacheBuilder<K, V> meterRegistry(MeterRegistry v)        { this.meterRegistry = v; return this; }

    public TieredCache<K, V> build(TieredCacheManager manager) {
        if (name == null || name.isBlank())  throw new IllegalStateException("name is required");
        if (loader == null)                  throw new IllegalStateException("loader is required");
        if (valueJavaType == null)           throw new IllegalStateException("valueType is required");

        MeterRegistry registry = enableMetrics
                ? (meterRegistry != null ? meterRegistry : manager.getMeterRegistry())
                : null;

        TieredCacheImpl<K, V> cache = new TieredCacheImpl<>(
                name, l1MaxSize, l1RefreshAfterWrite,
                l2KeyPrefix, l2Ttl, l2TtlJitter, keySchemaVersion,
                nullValueTtl, hotspotProtection,
                loaderFailure, redisReadFailure, redisWriteFailure, deserializeFailure,
                loader, valueJavaType, objectMapper,
                manager.getRedis(), manager.getReactiveRedis(), registry
        );
        manager.register(cache);
        return cache;
    }
}
```

- [ ] **Step 2：编译验证**

```bash
cd backend && mvn compile -pl canvas-cache-sdk -q
```

Expected: `BUILD SUCCESS`

- [ ] **Step 3：Commit**

```bash
git add backend/canvas-cache-sdk/src/main/java/org/chovy/cache/TieredCacheBuilder.java
git commit -m "feat(cache-sdk): add complete TieredCacheBuilder with all strategy options"
```

---

## Task 6：TieredCacheManager + AutoConfiguration

- [ ] **Step 1：TieredCacheManager.java**

```java
package org.chovy.cache;

import io.micrometer.core.instrument.MeterRegistry;
import jakarta.annotation.PostConstruct;
import lombok.Getter;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.data.redis.connection.ReactiveRedisConnectionFactory;
import org.springframework.data.redis.core.ReactiveStringRedisTemplate;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.data.redis.listener.PatternTopic;
import org.springframework.data.redis.listener.ReactiveRedisMessageListenerContainer;

import java.util.Map;
import java.util.Optional;
import java.util.concurrent.ConcurrentHashMap;

@Slf4j
@RequiredArgsConstructor
public class TieredCacheManager {

    @Getter private final StringRedisTemplate redis;
    @Getter private final ReactiveStringRedisTemplate reactiveRedis;
    @Getter private final MeterRegistry meterRegistry;
    private final ReactiveRedisConnectionFactory reactiveFactory;

    private final Map<String, TieredCacheImpl<?, ?>> caches = new ConcurrentHashMap<>();

    @PostConstruct
    void subscribeInvalidations() {
        try {
            ReactiveRedisMessageListenerContainer container =
                    new ReactiveRedisMessageListenerContainer(reactiveFactory);
            container.receive(PatternTopic.of("tiered-cache:*:invalidate"))
                    .subscribe(
                            msg -> {
                                String channel = msg.getChannel();
                                String rawKey  = msg.getMessage();
                                String[] parts = channel.split(":");
                                if (parts.length >= 3) {
                                    TieredCacheImpl<?, ?> cache = caches.get(parts[1]);
                                    if (cache != null) cache.onInvalidateBroadcast(rawKey);
                                }
                            },
                            e -> log.error("[TIERED_CACHE_MANAGER] Pub/Sub error: {}", e.getMessage())
                    );
            log.info("[TIERED_CACHE_MANAGER] 订阅 tiered-cache:*:invalidate");
        } catch (Exception e) {
            log.warn("[TIERED_CACHE_MANAGER] Pub/Sub 订阅失败（Redis 不可用时忽略）: {}", e.getMessage());
        }
    }

    public void register(TieredCacheImpl<?, ?> cache) {
        caches.put(cache.getName(), cache);
        log.info("[TIERED_CACHE_MANAGER] 注册缓存 name={}", cache.getName());
    }

    public Optional<TieredCache<?, ?>> getCache(String name) {
        return Optional.ofNullable(caches.get(name));
    }
}
```

- [ ] **Step 2：AutoConfiguration + imports 文件**

`autoconfigure/TieredCacheAutoConfiguration.java`:
```java
package org.chovy.cache.autoconfigure;

import io.micrometer.core.instrument.MeterRegistry;
import org.chovy.cache.TieredCacheManager;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.context.annotation.Bean;
import org.springframework.data.redis.connection.ReactiveRedisConnectionFactory;
import org.springframework.data.redis.core.ReactiveStringRedisTemplate;
import org.springframework.data.redis.core.StringRedisTemplate;

@AutoConfiguration
public class TieredCacheAutoConfiguration {

    @Bean
    @ConditionalOnMissingBean
    public TieredCacheManager tieredCacheManager(StringRedisTemplate redis,
                                                   ReactiveStringRedisTemplate reactiveRedis,
                                                   ReactiveRedisConnectionFactory factory,
                                                   MeterRegistry meterRegistry) {
        return new TieredCacheManager(redis, reactiveRedis, meterRegistry, factory);
    }
}
```

`resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`:
```
org.chovy.cache.autoconfigure.TieredCacheAutoConfiguration
```

- [ ] **Step 3：编译 + Commit**

```bash
cd backend && mvn compile -pl canvas-cache-sdk -q
git add backend/canvas-cache-sdk/src/main/java/org/chovy/cache/TieredCacheManager.java \
        backend/canvas-cache-sdk/src/main/java/org/chovy/cache/autoconfigure/ \
        backend/canvas-cache-sdk/src/main/resources/
git commit -m "feat(cache-sdk): add TieredCacheManager with Pub/Sub and AutoConfiguration"
```

---

## Task 7：方法级显式注解 API（不做 Mapper 类级自动缓存）

**Files:** `annotation/`, `aop/`, `autoconfigure/TieredCacheAutoConfiguration.java`

约束：

- 只支持方法级注解：`@TieredCached`、`@TieredCacheEvict`、`@TieredCachePut`。
- 不实现 `@TieredCacheMapper`，不对 Mapper 类做全量自动缓存。
- 不自动缓存继承自 `BaseMapper` 的所有方法；复杂查询必须显式声明具体方法和 key。
- 第一版注解只支持同步返回值；响应式方法继续使用 `ReactiveTieredCache` 手动接入。

- [ ] **Step 1：创建 `annotation/TieredCached.java`**

```java
package org.chovy.cache.annotation;

import org.chovy.cache.strategy.*;
import java.lang.annotation.*;

@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface TieredCached {
    String name();
    String key();
    Class<?> valueType();

    int l1MaxSize() default 1000;
    String l1RefreshAfterWrite() default "1h";
    String l2KeyPrefix() default "cache:";
    String l2Ttl() default "24h";
    double l2TtlJitter() default 0.1;
    int keySchemaVersion() default 1;
    String nullValueTtl() default "5m";
    boolean hotspotProtection() default false;

    LoaderFailureStrategy onLoaderFailure() default LoaderFailureStrategy.THROW;
    RedisFailureStrategy onRedisReadFailure() default RedisFailureStrategy.FALLTHROUGH;
    RedisFailureStrategy onRedisWriteFailure() default RedisFailureStrategy.FALLTHROUGH;
    DeserializeFailureStrategy onDeserializeFailure() default DeserializeFailureStrategy.FALLTHROUGH_TO_L3;
}
```

- [ ] **Step 2：创建 `annotation/TieredCacheEvict.java`**

```java
package org.chovy.cache.annotation;

import java.lang.annotation.*;

@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface TieredCacheEvict {
    String name();
    String key();
    boolean beforeInvocation() default false;
    boolean afterCommit() default true;
}
```

- [ ] **Step 3：创建 `annotation/TieredCachePut.java`**

```java
package org.chovy.cache.annotation;

import java.lang.annotation.*;

@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface TieredCachePut {
    String name();
    String key();
    boolean afterCommit() default true;
}
```

`@TieredCachePut` 只操作已存在的命名缓存；如果找不到同名缓存，调用时快速失败。

- [ ] **Step 4：创建 `aop/SpelKeyEvaluator.java`**

```java
package org.chovy.cache.aop;

import org.springframework.core.DefaultParameterNameDiscoverer;
import org.springframework.expression.ExpressionParser;
import org.springframework.expression.spel.standard.SpelExpressionParser;
import org.springframework.expression.spel.support.StandardEvaluationContext;

import java.lang.reflect.Method;

public class SpelKeyEvaluator {
    private final ExpressionParser parser = new SpelExpressionParser();
    private final DefaultParameterNameDiscoverer nameDiscoverer = new DefaultParameterNameDiscoverer();

    public Object evaluate(String expression, Method method, Object[] args) {
        StandardEvaluationContext context = new StandardEvaluationContext();
        String[] names = nameDiscoverer.getParameterNames(method);
        for (int i = 0; i < args.length; i++) {
            context.setVariable("p" + i, args[i]);
            context.setVariable("a" + i, args[i]);
            if (names != null && i < names.length) {
                context.setVariable(names[i], args[i]);
            }
        }
        return parser.parseExpression(expression).getValue(context);
    }
}
```

- [ ] **Step 5：创建 `aop/AnnotationCacheResolver.java`**

```java
package org.chovy.cache.aop;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.chovy.cache.*;
import org.chovy.cache.annotation.TieredCached;
import org.springframework.boot.convert.DurationStyle;

import java.time.Duration;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

public class AnnotationCacheResolver {
    private final TieredCacheManager manager;
    private final ObjectMapper objectMapper;
    private final Map<String, TieredCache<Object, Object>> caches = new ConcurrentHashMap<>();

    public AnnotationCacheResolver(TieredCacheManager manager, ObjectMapper objectMapper) {
        this.manager = manager;
        this.objectMapper = objectMapper;
    }

    public TieredCache<Object, Object> resolve(TieredCached annotation) {
        return caches.computeIfAbsent(annotation.name(), ignored ->
                TieredCacheBuilder.<Object, Object>builder()
                        .name(annotation.name())
                        .l1MaxSize(annotation.l1MaxSize())
                        .l1RefreshAfterWrite(parse(annotation.l1RefreshAfterWrite()))
                        .l2KeyPrefix(annotation.l2KeyPrefix())
                        .l2Ttl(parse(annotation.l2Ttl()))
                        .l2TtlJitter(annotation.l2TtlJitter())
                        .keySchemaVersion(annotation.keySchemaVersion())
                        .nullValueTtl(parse(annotation.nullValueTtl()))
                        .hotspotProtection(annotation.hotspotProtection())
                        .onLoaderFailure(annotation.onLoaderFailure())
                        .onRedisReadFailure(annotation.onRedisReadFailure())
                        .onRedisWriteFailure(annotation.onRedisWriteFailure())
                        .onDeserializeFailure(annotation.onDeserializeFailure())
                        // 注解缓存的 L3 Loader 来自每次方法调用的 loaderOverride。
                        .loader(key -> { throw new IllegalStateException("Use get(key, loaderOverride) for annotation cache"); })
                        .valueType((Class<Object>) annotation.valueType())
                        .objectMapper(objectMapper)
                        .build(manager));
    }

    @SuppressWarnings("unchecked")
    public TieredCache<Object, Object> getExisting(String name) {
        TieredCache<Object, Object> cache = caches.get(name);
        if (cache == null) {
            cache = (TieredCache<Object, Object>) manager.getCache(name)
                    .orElseThrow(() -> new IllegalStateException("Unknown tiered cache: " + name));
        }
        return cache;
    }

    private Duration parse(String value) {
        return DurationStyle.detectAndParse(value);
    }
}
```

- [ ] **Step 6：TieredCacheManager 暴露按 name 查询**

在 `TieredCacheManager` 中新增：

```java
public Optional<TieredCache<?, ?>> getCache(String name) {
    return Optional.ofNullable(caches.get(name));
}
```

同时将 `register(TieredCacheImpl<?, ?> cache)` 改为 `public`，便于注解解析器和手动 Builder 统一注册。

- [ ] **Step 7：创建 `aop/TieredCacheAspect.java`**

```java
package org.chovy.cache.aop;

import lombok.RequiredArgsConstructor;
import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.*;
import org.aspectj.lang.reflect.MethodSignature;
import org.chovy.cache.TieredCache;
import org.chovy.cache.annotation.*;
import org.springframework.transaction.support.TransactionSynchronization;
import org.springframework.transaction.support.TransactionSynchronizationManager;

import java.lang.reflect.Method;

@Aspect
@RequiredArgsConstructor
public class TieredCacheAspect {
    private final AnnotationCacheResolver resolver;
    private final SpelKeyEvaluator keyEvaluator;

    @Around("@annotation(annotation)")
    public Object aroundCached(ProceedingJoinPoint pjp, TieredCached annotation) {
        Method method = ((MethodSignature) pjp.getSignature()).getMethod();
        Object key = keyEvaluator.evaluate(annotation.key(), method, pjp.getArgs());
        TieredCache<Object, Object> cache = resolver.resolve(annotation);
        return cache.get(key, () -> proceed(pjp)).orElse(null);
    }

    @Around("@annotation(annotation)")
    public Object aroundEvict(ProceedingJoinPoint pjp, TieredCacheEvict annotation) {
        Method method = ((MethodSignature) pjp.getSignature()).getMethod();
        Object key = keyEvaluator.evaluate(annotation.key(), method, pjp.getArgs());
        if (annotation.beforeInvocation()) {
            resolver.getExisting(annotation.name()).invalidate(key);
            return proceed(pjp);
        }
        Object result = proceed(pjp);
        runAfterCommit(annotation.afterCommit(),
                () -> resolver.getExisting(annotation.name()).invalidate(key));
        return result;
    }

    @Around("@annotation(annotation)")
    public Object aroundPut(ProceedingJoinPoint pjp, TieredCachePut annotation) {
        Method method = ((MethodSignature) pjp.getSignature()).getMethod();
        Object key = keyEvaluator.evaluate(annotation.key(), method, pjp.getArgs());
        Object result = proceed(pjp);
        runAfterCommit(annotation.afterCommit(),
                () -> resolver.getExisting(annotation.name()).put(key, result));
        return result;
    }

    private Object proceed(ProceedingJoinPoint pjp) {
        try {
            return pjp.proceed();
        } catch (Throwable e) {
            if (e instanceof RuntimeException re) throw re;
            throw new RuntimeException(e);
        }
    }

    private void runAfterCommit(boolean afterCommit, Runnable action) {
        if (afterCommit && TransactionSynchronizationManager.isSynchronizationActive()) {
            TransactionSynchronizationManager.registerSynchronization(new TransactionSynchronization() {
                @Override public void afterCommit() { action.run(); }
            });
        } else {
            action.run();
        }
    }
}
```

- [ ] **Step 8：AutoConfiguration 注册注解层 Bean**

在 `TieredCacheAutoConfiguration.java` 中新增：

```java
@Bean
@ConditionalOnMissingBean
public SpelKeyEvaluator spelKeyEvaluator() {
    return new SpelKeyEvaluator();
}

@Bean
@ConditionalOnMissingBean
public AnnotationCacheResolver annotationCacheResolver(TieredCacheManager manager,
                                                       ObjectMapper objectMapper) {
    return new AnnotationCacheResolver(manager, objectMapper);
}

@Bean
@ConditionalOnMissingBean
public TieredCacheAspect tieredCacheAspect(AnnotationCacheResolver resolver,
                                           SpelKeyEvaluator keyEvaluator) {
    return new TieredCacheAspect(resolver, keyEvaluator);
}
```

- [ ] **Step 9：编译 + Commit**

```bash
cd backend && mvn compile -pl canvas-cache-sdk -q
git add backend/canvas-cache-sdk/src/main/java/org/chovy/cache/annotation/ \
        backend/canvas-cache-sdk/src/main/java/org/chovy/cache/aop/ \
        backend/canvas-cache-sdk/src/main/java/org/chovy/cache/TieredCacheManager.java \
        backend/canvas-cache-sdk/src/main/java/org/chovy/cache/autoconfigure/TieredCacheAutoConfiguration.java
git commit -m "feat(cache-sdk): add explicit method-level cache annotations"
```

---

## Task 8：测试工具

- [ ] **Step 1：InMemoryTieredCache.java**

```java
package org.chovy.cache.testing;

import org.chovy.cache.ReactiveTieredCache;
import org.chovy.cache.TieredCache;
import reactor.core.publisher.Mono;

import java.util.Optional;
import java.util.concurrent.ConcurrentHashMap;
import java.util.function.Function;
import java.util.function.Supplier;

/** 测试用内存实现：L2 = ConcurrentHashMap，无 Pub/Sub，无网络依赖 */
public class InMemoryTieredCache<K, V> implements TieredCache<K, V> {

    private final String name;
    private final ConcurrentHashMap<K, Optional<V>> store = new ConcurrentHashMap<>();
    private final Function<K, V> loader;

    private InMemoryTieredCache(String name, Function<K, V> loader) {
        this.name = name;
        this.loader = loader;
    }

    public static <K, V> InMemoryTieredCache<K, V> of(Function<K, V> loader) {
        return new InMemoryTieredCache<>("test", loader);
    }

    public static <K, V> InMemoryTieredCache<K, V> of(String name, Function<K, V> loader) {
        return new InMemoryTieredCache<>(name, loader);
    }

    @Override public String name() { return name; }

    @Override
    public Optional<V> get(K key) {
        return store.computeIfAbsent(key, k -> Optional.ofNullable(loader.apply(k)));
    }

    @Override
    public Optional<V> get(K key, Supplier<V> loaderOverride) {
        return store.computeIfAbsent(key, k -> Optional.ofNullable(loaderOverride.get()));
    }

    @Override public void put(K key, V value)   { store.put(key, Optional.ofNullable(value)); }
    @Override public void invalidate(K key)     { store.remove(key); }
    @Override public void refresh(K key)        { store.remove(key); get(key); }

    @Override
    public void safeWrite(K key, Runnable writeAction, long delayMs) {
        store.remove(key);
        writeAction.run();
        store.remove(key);
    }

    @Override
    public ReactiveTieredCache<K, V> asReactive() {
        InMemoryTieredCache<K, V> self = this;
        return new ReactiveTieredCache<>() {
            public Mono<Optional<V>> get(K key)    { return Mono.fromCallable(() -> self.get(key)); }
            public Mono<Void> put(K key, V value)  { return Mono.fromRunnable(() -> self.put(key, value)).then(); }
            public Mono<Void> invalidate(K key)    { return Mono.fromRunnable(() -> self.invalidate(key)).then(); }
            public Mono<Void> refresh(K key)       { return Mono.fromRunnable(() -> self.refresh(key)).then(); }
        };
    }
}
```

- [ ] **Step 2：@TestTieredCache 注解 + 自动装配**

`testing/TestTieredCacheConfig.java`:
```java
package org.chovy.cache.testing;

import org.chovy.cache.TieredCache;
import org.springframework.boot.test.context.TestConfiguration;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Primary;

import java.lang.annotation.*;

/**
 * 标注此注解后，Spring context 中所有 TieredCache Bean 替换为 InMemoryTieredCache。
 * 测试用例无需改动注入代码，与生产代码使用相同接口。
 */
@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
@Documented
@TestConfiguration
public @interface TestTieredCache {}
```

- [ ] **Step 3：编译 + Commit**

```bash
cd backend && mvn compile -pl canvas-cache-sdk -q
git add backend/canvas-cache-sdk/src/main/java/org/chovy/cache/testing/
git commit -m "feat(cache-sdk): add InMemoryTieredCache and @TestTieredCache for unit testing"
```

---

## Task 9：单元测试

- [ ] **Step 1：创建测试类，覆盖所有关键路径**

创建 `src/test/java/org/chovy/cache/TieredCacheTest.java`，测试以下场景：

1. L3 Loader 只在 L1+L2 全 miss 时调用
2. L2 命中时不调 Loader
3. invalidate 后再 get 触发重新加载
4. **穿透防护**：Loader 返回 null 写哨兵，下次不调 Loader
5. **雪崩防护**：TTL 在 `[l2Ttl, l2Ttl*(1+jitter)]` 范围内随机分布
6. **策略测试**：`RETURN_STALE` 时 Loader 失败返回旧值
7. **Redis 读失败降级**：`FALLTHROUGH` 时穿透 L3；`FAIL_FAST` 时抛出
8. **双删一致性**：`safeWrite` 执行前后各删一次 L2
9. **响应式视图**：`asReactive().get(key)` 返回正确 `Mono<Optional<V>>`
10. **方法级注解**：`@TieredCached` 第二次调用不执行原方法；`@TieredCacheEvict(afterCommit=true)` 在事务提交后失效；`@TieredCachePut` 写入返回值

- [ ] **Step 2：运行测试**

```bash
cd backend && mvn test -pl canvas-cache-sdk -q
```

Expected: 所有测试通过

- [ ] **Step 3：Commit**

```bash
git add backend/canvas-cache-sdk/src/test/
git commit -m "test(cache-sdk): cover cache strategies, protections, and annotations"
```

---

## Task 10：迁移 CanvasConfigCache

- [ ] **Step 1：创建 CacheConfig.java**

```java
package org.chovy.canvas.config;

import org.chovy.cache.TieredCache;
import org.chovy.cache.TieredCacheBuilder;
import org.chovy.cache.TieredCacheManager;
import org.chovy.cache.strategy.LoaderFailureStrategy;
import org.chovy.canvas.domain.canvas.CanvasVersionMapper;
import org.chovy.canvas.engine.dag.DagGraph;
import org.chovy.canvas.engine.dag.DagParser;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import java.time.Duration;

@Configuration
public class CacheConfig {

    @Bean
    TieredCache<Long, DagGraph> canvasConfigCache(TieredCacheManager manager,
                                                   CanvasVersionMapper mapper,
                                                   DagParser parser) {
        return TieredCacheBuilder.<Long, DagGraph>builder()
                .name("canvas-config")
                .l1MaxSize(500)
                .l1RefreshAfterWrite(Duration.ofHours(2))
                .l2KeyPrefix("canvas:config:")
                .l2Ttl(Duration.ofHours(24))
                .l2TtlJitter(0.1)
                .keySchemaVersion(1)
                .nullValueTtl(Duration.ofMinutes(5))
                .onLoaderFailure(LoaderFailureStrategy.THROW)
                .loader(versionId -> {
                    var v = mapper.selectById(versionId);
                    if (v == null || v.getGraphJson() == null || v.getGraphJson().isBlank())
                        throw new IllegalStateException("版本 " + versionId + " 的 graph_json 不可用");
                    return parser.parse(v.getGraphJson());
                })
                .valueType(DagGraph.class)
                .build(manager);
    }
}
```

- [ ] **Step 2：重写 CanvasConfigCache 为薄包装**

```java
@Service
@RequiredArgsConstructor
public class CanvasConfigCache {
    private final TieredCache<Long, DagGraph> canvasConfigCache;

    public DagGraph get(Long canvasId, Long versionId) {
        return canvasConfigCache.getOrThrow(versionId);
    }
    public void invalidate(Long canvasId, Long versionId) {
        canvasConfigCache.invalidate(versionId);
    }
    public void evictL1(String l1Key) { /* 兼容性保留，SDK 的 invalidate 已包含 L1 */ }
}
```

- [ ] **Step 3：全量测试**

```bash
cd backend && mvn test -q
```

Expected: `BUILD SUCCESS`

- [ ] **Step 4：Commit**

```bash
git add backend/canvas-engine/src/main/java/org/chovy/canvas/config/CacheConfig.java \
        backend/canvas-engine/src/main/java/org/chovy/canvas/infra/cache/CanvasConfigCache.java
git commit -m "refactor: migrate CanvasConfigCache to canvas-cache-sdk"
```
