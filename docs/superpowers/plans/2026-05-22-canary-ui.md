# 灰度发布 UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在画布编辑页顶栏补全灰度发布 UI：开始灰度（带比例 Slider 的弹窗）、灰度状态 Badge、晋升全量、回滚灰度。

**Architecture:** 纯前端改动——TypeScript 类型补字段 → api.ts 补 rollbackCanary → 编辑页顶栏条件渲染灰度控件 + 弹窗逻辑。

**Tech Stack:** React 18, TypeScript, Ant Design (Slider, Modal, Tag, Tooltip)

---

## File Map

| Action | File |
|--------|------|
| Modify | `frontend/src/types/index.ts` |
| Modify | `frontend/src/services/api.ts` |
| Modify | `frontend/src/pages/canvas-editor/index.tsx` |

---

## Task 1：补充 TypeScript 类型和 API

- [ ] **Step 1：types/index.ts 补灰度字段**

在 `Canvas` interface 中，`editVersion?: number` 之后追加：

```typescript
canaryVersionId?: number
canaryPercent?: number
```

- [ ] **Step 2：api.ts 补 rollbackCanary**

在 `promoteCanary` 之后添加：

```typescript
rollbackCanary: (id: number) =>
  http.post<R<void>, R<void>>(`/canvas/${id}/rollback-canary`),
```

- [ ] **Step 3：验证 TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: 无报错

- [ ] **Step 4：Commit**

```bash
git add frontend/src/types/index.ts frontend/src/services/api.ts
git commit -m "feat: add canary fields to Canvas type and rollbackCanary API"
```

---

## Task 2：编辑页灰度状态 State + Handler

**Files:**
- Modify: `frontend/src/pages/canvas-editor/index.tsx`

- [ ] **Step 1：添加灰度相关 State（在现有 State 声明区，约 L200 附近）**

```typescript
const [canaryModalOpen, setCanaryModalOpen] = useState(false)
const [canaryPercent, setCanaryPercent]     = useState(20)
```

- [ ] **Step 2：添加三个灰度 Handler（在 handleSave/handlePublish 之后）**

```typescript
const handleStartCanary = async () => {
  const hasExisting = !!detail.canvas.canaryVersionId
  if (hasExisting) {
    const confirmed = await new Promise<boolean>(resolve =>
      Modal.confirm({
        title: '覆盖灰度版本',
        content: `当前已有灰度版本（${detail.canvas.canaryPercent}%），确认覆盖？`,
        okText: '确认覆盖',
        okButtonProps: { danger: true },
        cancelText: '取消',
        onOk: () => resolve(true),
        onCancel: () => resolve(false),
      })
    )
    if (!confirmed) return
  }
  await canvasApi.canary(canvasId, canaryPercent)
  message.success(`灰度发布成功，${canaryPercent}% 用户将收到新版本`)
  setCanaryModalOpen(false)
  window.location.reload() // 刷新 detail 获取最新 canaryVersionId
}

const handlePromoteCanary = () => {
  Modal.confirm({
    title: '晋升灰度为全量',
    content: '灰度版本将成为正式版本，所有用户切换到新版本。确认晋升？',
    okText: '确认晋升',
    onOk: async () => {
      await canvasApi.promoteCanary(canvasId)
      message.success('已晋升为全量版本')
      window.location.reload()
    },
  })
}

const handleRollbackCanary = () => {
  Modal.confirm({
    title: '回滚灰度',
    content: '灰度版本将被丢弃，所有用户恢复到正式版本。确认回滚？',
    okType: 'danger',
    okText: '确认回滚',
    onOk: async () => {
      await canvasApi.rollbackCanary(canvasId)
      message.warning('灰度已回滚')
      window.location.reload()
    },
  })
}
```

- [ ] **Step 3：Commit**

```bash
git add frontend/src/pages/canvas-editor/index.tsx
git commit -m "feat: add canary state and handlers in canvas editor"
```

---

## Task 3：顶栏灰度 UI 控件

**Files:**
- Modify: `frontend/src/pages/canvas-editor/index.tsx`

- [ ] **Step 1：补充 import**

在顶部 antd import 中加入 `Slider`、`Tag`（若未引入）：

```typescript
import {
  Button, Modal, Form, Input, message, Typography, Tooltip,
  Dropdown, DatePicker, InputNumber, Divider, Slider, Tag,
} from 'antd'
```

- [ ] **Step 2：定位顶栏区域（约 L785-795）并替换发布/灰度按钮区**

找到现有的发布按钮区，将其替换为条件渲染逻辑：

```tsx
{/* 灰度状态 / 发布区域 */}
{status === 1 && detail.canvas.canaryVersionId ? (
  // 灰度中状态
  <Space>
    <Tag color="orange" style={{ fontSize: 13, padding: '2px 10px' }}>
      🟡 灰度中 {detail.canvas.canaryPercent}%
    </Tag>
    <Tooltip title="将灰度版本晋升为全量正式版本">
      <Button size="small" type="primary" onClick={handlePromoteCanary}>
        晋升全量
      </Button>
    </Tooltip>
    <Tooltip title="丢弃灰度版本，恢复全量走正式版本">
      <Button size="small" danger onClick={handleRollbackCanary}>
        回滚
      </Button>
    </Tooltip>
  </Space>
) : (
  // 无灰度状态
  <Space>
    {status !== 1 && isAdmin && (
      <Button size="small" type="primary" icon={<CloudUploadOutlined />}
        onClick={() => handlePublish()}>
        发布
      </Button>
    )}
    {status === 1 && isAdmin && (
      <Tooltip title={isDirty ? '请先保存草稿再灰度发布' : '将当前草稿作为灰度版本发布'}>
        <Button size="small" icon={<ExperimentOutlined />}
          disabled={isDirty}
          onClick={() => setCanaryModalOpen(true)}>
          灰度发布
        </Button>
      </Tooltip>
    )}
  </Space>
)}
```

添加 icon import：`ExperimentOutlined`

- [ ] **Step 3：在 JSX 末尾（Modal 区域）添加灰度发布弹窗**

```tsx
{/* 灰度发布弹窗 */}
<Modal
  title="灰度发布"
  open={canaryModalOpen}
  onOk={handleStartCanary}
  onCancel={() => setCanaryModalOpen(false)}
  okText="确认灰度发布"
  width={460}
>
  <div style={{ padding: '8px 0' }}>
    <div style={{ marginBottom: 8, fontWeight: 500 }}>
      灰度比例：<span style={{ color: '#1890ff', fontSize: 18 }}>{canaryPercent}%</span>
    </div>
    <Slider
      min={1} max={99} value={canaryPercent}
      onChange={setCanaryPercent}
      marks={{ 1: '1%', 10: '10%', 30: '30%', 50: '50%', 99: '99%' }}
      style={{ marginBottom: 24 }}
    />
    <div style={{ color: '#8c8c8c', fontSize: 12, lineHeight: '20px' }}>
      <div>· {canaryPercent}% 的用户将收到新版本画布</div>
      <div>· 用户分配基于 hash，同一用户始终命中同一版本</div>
      <div>· 灰度期间可随时晋升全量或回滚</div>
    </div>
  </div>
</Modal>
```

- [ ] **Step 4：验证 TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: 无报错

- [ ] **Step 5：手动测试**

启动 dev server（`npm run dev`），测试以下路径：

1. 已发布画布 → 编辑页顶栏显示"灰度发布"按钮（未保存草稿时置灰）
2. 保存草稿后 → "灰度发布"可点击 → 弹窗出现 Slider
3. 拖动 Slider 选择 20% → 确认 → 提示"灰度发布成功" → 页面刷新
4. 刷新后顶栏显示 🟡 灰度中 20% + 晋升全量 + 回滚
5. 点击"晋升全量" → 确认弹窗 → 确认 → 页面刷新 → 灰度 Badge 消失
6. 重新灰度后点击"回滚" → 确认弹窗 → 确认 → 页面刷新 → 灰度 Badge 消失

- [ ] **Step 6：Commit**

```bash
git add frontend/src/pages/canvas-editor/index.tsx
git commit -m "feat: add canary UI to canvas editor — badge, start modal, promote and rollback"
```
