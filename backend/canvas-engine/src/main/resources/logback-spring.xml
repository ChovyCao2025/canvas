<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 结构化 JSON 日志（设计文档 22.3 节） -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"canvas-engine"}</customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <level>level</level>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
            </fieldNames>
            <!-- 保留 MDC 字段（traceId / canvasId / userId / nodeId 等） -->
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>canvasId</includeMdcKeyName>
            <includeMdcKeyName>nodeId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
        </encoder>
    </appender>

    <!-- 开发环境：可读文本格式 -->
    <appender name="TEXT_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!prod">
        <logger name="org.chovy.canvas" level="DEBUG"/>
        <root level="INFO">
            <appender-ref ref="TEXT_CONSOLE"/>
        </root>
    </springProfile>

</configuration>
