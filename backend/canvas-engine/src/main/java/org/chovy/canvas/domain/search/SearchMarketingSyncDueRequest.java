package org.chovy.canvas.domain.search;

/**
 * SearchMarketingSyncDueRequest 承载 domain.search 场景中的不可变数据快照。
 * @param limit limit 字段。
 */
public record SearchMarketingSyncDueRequest(Integer limit) {
}
